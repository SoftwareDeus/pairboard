export type Card = {
    id: string;
    text: string;
    type: string;
};

export type ListEntry = {
    id: string;
    listId: string;
    name: string;
    created_at: string;
    updated_at: string;
};